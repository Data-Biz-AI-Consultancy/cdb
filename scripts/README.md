# CDB Operational Scripts

This directory is reserved for operational utilities and maintenance scripts for CDB.

> [!NOTE]
> Historical migration from Jager CDP has completed and legacy migration scripts have been retired. All data ingestion is now managed dynamically via REST API endpoints under `/api/v1/ingest/` authenticated via API keys.

